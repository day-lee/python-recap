Concurrency & Async 동시성 & 비동기 프로그래밍

프로그램 ->프로세스-> 스레드  
SSD   ->  RAM -> CPU

프로그램
- 프로젝트의 "설계도" python.exe (blueprint)
- SSD, HDD 창고에 저장되어 있음

프로세스
- OS에게서 독립된 메모리(작업대) RAM 공간을 할당받음. 
- "설계도가 메모리에 올라가서 실행 중인 상태". 프로그램의 인스턴스. (an active instance of a program loaded into memory)

스레드 
- "CPU가 코드를 실행하는 최소 단위". (smallest unit of execution by the CPU)
- 프로세스 내부에서 실제 일을 함. 
- 프로세스 내 메모리를 공유함. (shares the memory space - code, heap, data within the process)
- 자신만의 스레드 stack을 가짐

┌────────────────────────────────────────────────────────┐
│ [운영체제 (OS) 메인 메모리]                                 │
│                                                        │
│  ┌──────────────────────────────────────────────────┐  │
│  │ 프로세스 A (예: 파이썬 프로그램)                        │  │
│  │                                                  │  │
│  │  공유 영역 (모든 직원이 같이 쓰는 공간)                   │  │
│  │  ┌────────────────────────────────────────────┐  │  │
│  │  │  - Code (파이썬 소스 코드)                     │  │  │
│  │  │  - Data (전역 변수)                          │  │  │
│  │  │  - Heap (메모리에 동적으로 생성된 객체들)          │  │  │
│  │  └────────────────────────────────────────────┘  │  │
│  │         │                     │                  │  │
│  │         ▼                     ▼                  │  │
│  │  ┌───────────────┐     ┌───────────────┐         │  │
│  │  │ 스레드 1 (직원1)│     │ 스레드 2 (직원2)│           │  │
│  │  │ - 나만의 Stack │     │ - 나만의 Stack │           │  │
│  │  └───────────────┘     └───────────────┘         │  │
│  └──────────────────────────────────────────────────┘  │
│                            ❌ 완전히 격리 (침범 불가)        │ 
│  ┌──────────────────────────────────────────────────┐  │
│  │ 프로세스 B (예: 크롬 브라우저)                          │  │
│  │  - 자신만의 고유한 Code / Data / Heap                 │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘

============================================================

파이썬의 고민/제약: GIL (Global Interpreter Lock)
- 여러 스레드가 프로세스의 데이터 영역(글로벌 변수, 객체 참조 횟수)를 공유 
- CPU가 멀티 코어로 동시에 공유 영역 값 바꾸려고 하면 데이터 꼬임 -> Race Condition 
- 이를 방지하려고 "공유 영역에 접근할 수 있는 권한(GIL) 키를 한 번에 한 스레드에게만 줌" 

- 파이썬 메모리 관리 방식을 지키기 위해 만든 설계 (Reference Counting)
- 파이썬은 CPU 코어 1개만 일한다. 
- GIL 키를 다른 스레드에 양도함. CPU 실행 권한이 스레드 하나에만 있음. 
- CPU 코어를 여러개 써서 멀티스레딩 해도 context switching 비용만 들고 아무 효과가 없다. 
- 자바스크립트는 태생이 싱글 스레드, 파이썬은 멀티 스레드가 가능하지만, 메모리 관리를 위해 제약을 둠


============================================================
프로그램이 느리다면...

1. I/O Bound (대기 시간 병목)
원인
- 네트워킹: API호출
- DB 쿼리 대기 
해결책 -> 1.threading / 2.asyncio 

2. CPU Bound (연산이 많아서)
원인
- 대용량 이미지 처리, 동영상 인코딩
- 암호화, 대규모 수학 연산
해결책 -> 3.multiprocessing (GIL 우회)
============================================================

1.Threading (I/O Bound)
- CPU가 놀고 있을 때 (api 기다리느 중..) 다른 스레드로 눈을 돌려 일 시킴. GIL이 있어도 I/O 대기 시간에는 자물쇠가 풀려서 효과있음
- 단점: 스레드가 너무 많으면 메모리 많이먹고, 데이터 경쟁(race condition)이 발생할 수 있어 다루기 까다로움

2.⭐️ Asyncio (I/O Bound)
- 싱글스레드 비동기: 스레드 1개로 기다리는 동안 다른 주문받는 식 
- threading 보다 메모리 적게 쓰고 안전함
- await로 자발적으로 제어권 양보하는 협력적, 비선점형 방식 


3. Multiprocessing(CPU Bound)
- GIL을 물리적으로 파괴해서 CPU 코어 개수 만큼 파이썬 프로세스를 돌림. 
- 프로세스 새로 띄우는 것은 메모리를 많이 먹고, 프로세스 같에 데이터를 주고 받는 비용이 크다. 

============================================================

🔄 비동기 순서도 
JS나 파이썬 비동기나 스레드 요리사 한 명이 일하는 구조야.
Call Stack  -> Background    -> Task Queue -> Event Loop -> Call Stack 
          (await 발견! 외부 위임)  (완료된 코루틴 줄세우기)

1. Call Stack과 Event Loop:
- 두 언어 모두 코드를 한 줄씩 실행하는 Call Stack(호출 스택)이 있어. 
- 실행하다가 await를 만나면(예: API 호출, 타이머), 엔진은 이 작업을 무겁게 쥐고 기다리지 않고 Web APIs / OS 커널에 백그라운드로 던져버려.
- 외부에서 작업이 끝나면 결과가 Task Queue(태스크 큐)에 들어가고, Event Loop가 눈치를 보다가 Call Stack이 텅 비는 순간 큐에 있던 작업을 스택으로 올려서 마저 실행하지.

2. 동기식 블로킹의 대참사 원리도 동일:
- 파이썬 비동기 서버에서 await 없이, time.sleep(5)를 쓰면, Event Loop 자체가 멈춰버려. 
- await 를 안붙이면 아예 비동기 작업을 시작하지 않음. 이벤트 루프가 인지하지 못해서 객체만 반환됨 
-> 경고: RuntimeWarning: coroutine 'fetch_data' was never awaited


==========================================================
asyncio 

코루틴
- 비동기 프로그램의 기본 단위. 실행을 중간에 멎췄다가 다시 실행할 수 있는 함수. 
- co 함께 협력하며 일하는, routine 루틴

1. 코루틴 함수 
    async def call_api(): ..
2. 코루틴 객체 
    my_coroutine = call_api() 
    이렇게 객체만 만들어두고 실행을 안하면 RuntimeWarning: coroutine was never awaited 라고 워닝이 나옴 

"""
======================================================================
[비동기 맛보기] 초급 연습 문제 - asyncio 기본기 다지기

asyncio.gather(함수1(), 함수2())
함수 실행 결과를 담은 리스트를 반환해준다. [함수1_결과, 함수2_결과]

"""

import asyncio
import time


async def question_1():
    """
    [1번 문제] 비동기 함수 선언하고 실행하기 (async/await)

    - 상황: 비동기로 동작하는 알람 기능을 만드려고 합니다.
    - 요구사항:
        1. '동기식'인 time.sleep(1) 대신, '비동기식' 대기 함수인 `asyncio.sleep(1)`을 사용하세요.
        2. 비동기 함수를 대기할 때는 반드시 앞에 `await` 키워드를 붙여야 합니다.
    - 출력 예시:
        알람 시작!
        (1초 대기)
        알람 울림: 삐빅!
    """
    print("알람 시작!")

    # TODO: 비동기 방식으로 1초 동안 대기하는 코드를 완성하세요.
    # 힌트: await asyncio.sleep(...)
    await asyncio.sleep(1)

    print("알람 울림: 삐빅!")


async def question_2():
    """
    [2번 문제] 두 개의 작업을 동시에 실행하기 (asyncio.gather)

    - 상황: 두 명의 요리사가 동시에 요리를 시작합니다. (A 요리사: 2초 소요, B 요리사: 1초 소요)
    - 요구사항:
        1. `chef_a`와 `chef_b` 두 개의 비동기 함수를 완성하세요. (각각 asyncio.sleep 활용)
        2. `asyncio.gather()`를 사용하여 두 작업을 '동시에(병렬로)' 실행시키세요.
    - 핵심 체크:
        동기 방식(일반 코드)이었다면 A가 끝나고 B가 하므로 총 '3초'가 걸리겠지만,
        비동기로 동시에 실행하면 총 소요 시간이 '2초'가 되어야 합니다.
    """

    async def chef_a():
        print("Chef A: 스테이크 굽기 시작 (2초 소요)")
        await asyncio.sleep(2)
        print("Chef A: 스테이크 완료!")

    async def chef_b():
        print("Chef B: 샐러드 만들기 시작 (1초 소요)")
        await asyncio.sleep(1)
        print("Chef B: 샐러드 완료!")

    start_time = time.time()

    # TODO: asyncio.gather를 사용해 chef_a()와 chef_b()를 동시에 실행하세요.
    # 예시: await asyncio.gather(함수1(), 함수2())
    await asyncio.gather(chef_a(), chef_b())
    end_time = time.time()
    print(f"총 소요 시간: {end_time - start_time:.2f}초")


# ======================================================================
# 비동기 함수(코루틴)는 일반 함수처럼 그냥 호출하면 실행되지 않습니다.
# 반드시 asyncio.run()이라는 '이벤트 루프' 구동 함수를 통해 실행해야 합니다.
# ======================================================================
if __name__ == "__main__":
    print("--- 1번 문제 실행 ---")
    asyncio.run(question_1())

    print("\n--- 2번 문제 실행 ---")
    asyncio.run(question_2())
